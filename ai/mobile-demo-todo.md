# Mobile Demo 收尾 TODO

当前 `npm run build` 已通过，未发现需要继续大改的阻塞问题。以下事项作为后续人工复核清单，不在本次收尾中继续扩展。

## 非阻塞检查项

- 在真实手机或浏览器设备模拟器中复核触控手感、折叠面板展开高度和 bottom sheet 内容可读性。
- 继续对照 `ai/mobile-data-contract.json` 抽查金额、比例、日期、评级、名单数量、舆情数量，确保后续改动不改写业务数字。
- 如后续桌面端 mock 数据继续调整，优先把仍散落在桌面组件内的 mock 常量集中导出，再同步移动端数据映射。
- 如业务侧要求隐藏更多桌面字段，仅做移动端展示隐藏，不修改 mock 原始值。
